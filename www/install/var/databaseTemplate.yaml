database:
  db_centreon:
    dsn: "mysql:host=--ADDRESS--:--DBPORT--;dbname=--CONFDB--"
    username: "--DBUSER--"
    password: "--DBPASS--"
  db_centstorage:
    dsn: "mysql:host=--ADDRESS--:--DBPORT--;dbname=--STORAGEDB--"
    username: "--DBUSER--"
    password: "--DBPASS--"
