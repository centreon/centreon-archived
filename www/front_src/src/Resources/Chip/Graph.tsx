import * as React from 'react';

import IconGraph from '@material-ui/icons/BarChart';

import Chip from '.';

const GraphChip = (): JSX.Element => <Chip icon={<IconGraph />} />;

export default GraphChip;
