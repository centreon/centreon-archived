centreon:
    resource: "./Centreon/**/*.yaml"
    prefix: "{base_uri}api/{version}"
    defaults:
        version: "latest"
    requirements:
        base_uri: ".*"
security:
    resource: "./Centreon/security/*.yaml"
    prefix: "{base_uri}"
    requirements:
        base_uri: ".*"
