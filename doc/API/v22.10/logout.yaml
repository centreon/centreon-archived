get:
  tags:
    - Authentication
  summary: "Logout"
  description: "Entry point to delete an existing authentication token."
  security:
    - Token: []
  responses:
    '200':
      description: "OK"
      content:
        application/json:
          schema:
            $ref: './Logout/Schema/authentication_response.yaml'
    '403':
      $ref: './Common/Response/forbidden.yaml'
    '500':
      $ref: './Common/Response/internal_server_error.yaml'