engines:
  phpcodesniffer:
    enabled: true
    config:
      file_extensions: "php,inc,lib"
      standard: "PSR1,PSR2"
      exclude_paths:
      - GPL_LIB/
      - features/
ratings:
  paths:
  - "**.php"
