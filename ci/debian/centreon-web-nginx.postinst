#!/bin/sh

if [ "$1" = "configure" ] ; then

  if [ -f "/etc/nginx/sites-available/centreon" ]; then
    # Remove default Debian Nginx configuration
    rm -f /etc/nginx/sites-enabled/default
    # Restart apache2
    systemctl restart nginx php8.0-fpm
  fi

fi

exit 0
